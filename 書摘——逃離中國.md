<!DOCTYPE html>
<html lang="zh-Hant">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>美國國會報導</title>
<style>
  /* 預設配色 */
  body {
    background-color: #DEDCDA;
    color: #8F9090;
    margin: 40px;
    font-family: "Microsoft JhengHei", sans-serif;
    line-height: 1.6;
    scroll-behavior: smooth; /* 平滑滾動 */
  }
  h1, h2, h3, h4 {
    color: #293342; /* 標題顏色 */
    margin-top: 2rem;
  }
  a {
    color: #C7955A; /* 連結顏色 */
    text-decoration: none;
  }
  a:hover {
    text-decoration: underline;
  }

  /* 響應式文字大小比例 */
  h1 { font-size: clamp(2rem, 4vw, 3rem); }
  h2 { font-size: clamp(1.8rem, 3.5vw, 2.5rem); }
  h3 { font-size: clamp(1.5rem, 3vw, 2rem); }
  h4 { font-size: clamp(1.2rem, 2.5vw, 1.5rem); }
  p  { font-size: clamp(1rem, 2vw, 1.2rem); }

  /* 分隔線 */
  .divider {
    border-top: 4px solid #313B4D;
    margin: 2rem 0;
  }

  /* 目錄樣式 */
  nav {
    background-color: rgba(49, 59, 77, 0.05);
    padding: 1rem;
    border-left: 4px solid #313B4D;
    margin-bottom: 2rem;
    position: sticky;
    top: 10px; /* 固定在視窗上方 */
    transition: max-height 0.3s ease;
  }
  nav h3 {
    margin-top: 0;
    color: #293342;
  }
  nav ul {
    list-style: none;
    padding-left: 0;
    margin: 0;
  }
  nav li {
    margin: 0.5rem 0;
  }
  nav a.active {
    font-weight: bold;
    background-color: rgba(199, 149, 90, 0.15);
    padding: 0.2rem 0.4rem;
    border-radius: 4px;
  }

  /* 手機版縮目錄 */
  .menu-toggle {
    display: none;
    background-color: #313B4D;
    color: white;
    padding: 0.5rem 1rem;
    border: none;
    font-size: 1rem;
    cursor: pointer;
    margin-bottom: 1rem;
    border-radius: 4px;
  }

  @media (max-width: 768px) {
    nav {
      max-height: 0;
      overflow: hidden;
      border-left: none;
      background-color: rgba(49, 59, 77, 0.1);
      padding: 0 1rem;
    }
    nav.open {
      max-height: 500px; /* 展開高度 */
      padding: 1rem;
    }
    .menu-toggle {
      display: inline-block;
    }
  }
</style>
</head>
<body>

<h1>美國國會報導</h1>

<!-- 手機版目錄按鈕 -->
<button class="menu-toggle">📑 展開目錄</button>

<!-- 目錄 -->
<nav>
  <h3>目錄</h3>
  <ul>
    <li><a href="#intro">序言</a></li>
    <li><a href="#origin">國會的起源</a></li>
    <li><a href="#system">國會兩院制度</a></li>
    <li><a href="#seats">席次變化歷史</a></li>
    <li><a href="#process">立法流程簡介</a></li>
  </ul>
</nav>

<h2 id="intro">序言</h2>
<p>2024年，台灣與美國都各自進行總統大選，而選後的結果也帶來震盪至今的餘波...</p>

<div class="divider"></div>

<h2 id="origin">國會的起源</h2>
<p>現在的美國國會已經是美國極為重要的象徵之一...</p>

<h4 id="system">國會兩院制度</h4>
<p>參議院：每州均有兩名參議員...</p>

<h4 id="seats">席次變化歷史</h4>
<p>眾議院席次從最初的65席逐漸增加...</p>

<h4 id="process">立法流程簡介</h4>
<p>1. 提出法案：任何一院議員均可提出法案...</p>

<script>
  // 滾動高亮
  const sections = document.querySelectorAll("h2, h4");
  const navLinks = document.querySelectorAll("nav a");
  window.addEventListener("scroll", () => {
    let current = "";
    sections.forEach(section => {
      const sectionTop = section.offsetTop - 100;
      if (pageYOffset >= sectionTop) {
        current = section.getAttribute("id");
      }
    });
    navLinks.forEach(link => {
      link.classList.remove("active");
      if (link.getAttribute("href") === "#" + current) {
        link.classList.add("active");
      }
    });
  });

  // 手機版展開/收合目錄
  const menuToggle = document.querySelector(".menu-toggle");
  const navMenu = document.querySelector("nav");
  menuToggle.addEventListener("click", () => {
    navMenu.classList.toggle("open");
    menuToggle.textContent = navMenu.classList.contains("open") ? "📑 收合目錄" : "📑 展開目錄";
  });
</script>

</body>
</html>
